import { PhaseStub } from '@/components/PhaseStub'

export default function SettingsPage() {
  return <PhaseStub to="/settings" body="Pharmacy details, invoice, GST, printers and payment methods" />
}
