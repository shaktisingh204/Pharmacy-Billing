import { PhaseStub } from '@/components/PhaseStub'

export default function Purchases() {
  return <PhaseStub to="/purchases" body="Goods receipt against supplier invoices, returns and short book" />
}
